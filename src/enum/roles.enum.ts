export enum Role {
  // Role for general users with basic access
  USER = 'USER',
  // Role for administrators with full access
  ADMIN = 'ADMIN',
  // Role for moderators with limited administrative access
  MODERATOR = 'MODERATOR',
}
